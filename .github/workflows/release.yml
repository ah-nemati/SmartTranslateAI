name: SmartTranslateAi - Cross Platform Enterprise Build System

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  PYTHON_VERSION: "3.11"
  APP_NAME: SmartTranslateAi
  LINUX_PACKAGE_NAME: smarttranslateai

jobs:
  # =========================================================
  # ANDROID FIRST (CRITICAL)
  # =========================================================
  android:
    name: Android Build (FIRST)
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install system dependencies
        run: |
          sudo apt-get update

          sudo apt-get install -y \
            git zip unzip wget curl \
            openjdk-17-jdk \
            build-essential \
            autoconf automake libtool pkg-config \
            zlib1g-dev libffi-dev libssl-dev \
            libncurses6 libtinfo6 libstdc++6

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip

          pip install --upgrade \
            buildozer \
            cython \
            python-for-android \
            kivy==2.3.0

      - name: Build Android APK
        run: |
          cd app
          buildozer android debug

      - name: Verify APK
        run: |
          find app/bin -name "*.apk" | grep apk || exit 1

      - name: Upload Android Artifact
        uses: actions/upload-artifact@v4
        with:
          name: android
          path: app/bin/*.apk

  # =========================================================
  # WINDOWS (ONLY IF ANDROID SUCCESS)
  # =========================================================
  windows:
    name: Windows Build
    runs-on: windows-latest
    needs: android

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build EXE
        shell: pwsh
        run: |
          pyinstaller `
            --clean `
            --noconfirm `
            --onefile `
            --windowed `
            --name "${{ env.APP_NAME }}" `
            --hidden-import=PySide6.QtCore `
            --hidden-import=PySide6.QtGui `
            --hidden-import=PySide6.QtWidgets `
            --hidden-import=PySide6.QtNetwork `
            --collect-all PySide6 `
            --collect-all aiohttp `
            app/main.py

      - name: Verify EXE
        shell: pwsh
        run: |
          if (!(Test-Path "dist/${{ env.APP_NAME }}.exe")) {
            Write-Error "Windows EXE not found"
            exit 1
          }

      - name: Upload Windows Artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows
          path: dist/${{ env.APP_NAME }}.exe

  # =========================================================
  # LINUX (ONLY IF WINDOWS SUCCESS)
  # =========================================================
  linux:
    name: Linux Build
    runs-on: ubuntu-24.04
    needs: windows

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install system libraries
        run: |
          sudo apt-get update

          sudo apt-get install -y \
            dpkg-dev fakeroot \
            libglib2.0-0 libgl1 libx11-6 libxext6 \
            libxrender1 libxcb1 libxcb-cursor0 \
            libxkbcommon-x11-0 libdbus-1-3 \
            libnss3 libfontconfig1 libfreetype6 \
            libegl1 libasound2t64

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build Linux Binary
        run: |
          pyinstaller \
            --clean \
            --noconfirm \
            --onefile \
            --windowed \
            --name "${{ env.APP_NAME }}" \
            --collect-all PySide6 \
            --collect-all aiohttp \
            app/main.py

      - name: Verify Binary
        run: |
          test -f dist/${{ env.APP_NAME }} || exit 1

      - name: Create DEB
        run: |
          mkdir -p package/usr/local/bin
          mkdir -p package/DEBIAN

          cp dist/${{ env.APP_NAME }} package/usr/local/bin/

          VERSION=${GITHUB_REF_NAME#v}

          cat > package/DEBIAN/control <<EOF
          Package: ${{ env.LINUX_PACKAGE_NAME }}
          Version: ${VERSION}
          Section: utils
          Priority: optional
          Architecture: amd64
          Maintainer: SmartTranslateAi
          Description: SmartTranslateAi Cross Platform Translator
          EOF

          dpkg-deb --build package \
            ${{ env.LINUX_PACKAGE_NAME }}-${VERSION}.deb

      - name: Upload Linux Artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux
          path: |
            dist/${{ env.APP_NAME }}
            *.deb

  # =========================================================
  # RELEASE (ONLY IF ALL SUCCESS)
  # =========================================================
  release:
    name: Release
    runs-on: ubuntu-latest

    needs: [android, windows, linux]

    if: |
      needs.android.result == 'success' &&
      needs.windows.result == 'success' &&
      needs.linux.result == 'success'

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Validate
        run: |
          find artifacts -type f

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: SmartTranslateAi ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            artifacts/android/*.apk
            artifacts/windows/*.exe
            artifacts/linux/*.deb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
